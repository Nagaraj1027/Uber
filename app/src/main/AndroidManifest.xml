<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uber">

    <application
        android:name=".uberapp.UberApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Uber">

        <activity
            android:name=".splash.SplashScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Uber.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".intent.FirstActivity" />
        <activity android:name=".intent.SecondActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".implicitintent.ImplicitIntentExample" />
        <activity android:name=".login.LoginActivity" />
    </application>

</manifest>